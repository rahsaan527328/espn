<% @messages.each do |message| %>
	<div class= "col-md_10 col_md_offset-1">
		<div class="message"></div>
			<h2 class="message-title"><%= message.title %></h2>
			<%= link_to "join converstion", message, class: "btn-custom" %>
		</div>
	</div> 

<% end %>
